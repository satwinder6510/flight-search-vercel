 {
   "version": 2,
   "builds": [
     { "src": "api/index.js",         "use": "@vercel/node"        },
     { "src": "package.json",         "use": "@vercel/static-build" }
   ],
-  "routes": [
+  "routes": [
     { "src": "/api/(.*)",  "dest": "api/index.js" },
     { "src": "/(.*)",       "dest": "/"           }
   ]
+  ,
+  // <<< Add this to install devDependencies too >>>
+  "installCommand": "npm install --include=dev"
 }
